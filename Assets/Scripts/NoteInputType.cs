using System;

/// <summary>
/// Enum de las posibles entradas que el jugador puede pulsar para evaluar una nota.
/// </summary>
public enum NoteInputType
{
    LB, // Left Button (color azul)
    LT, // Left Trigger (color azul)
    RB, // Right Button (color rojo)
    RT  // Right Trigger (color rojo)
}
